// serve-dist-and-smoke.js - disabled in production cleanup
// This development helper served the built `dist` directory and ran smoke checks.
// It's neutralized in production to keep the codebase minimal.
console.log('serve-dist-and-smoke.js is disabled in production bundle')
process.exit(0)
