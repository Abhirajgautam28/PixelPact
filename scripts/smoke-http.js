// smoke-http.js - disabled in production cleanup
// Previously used to check dev/preview servers; replaced with a harmless stub.
console.log('smoke-http.js is disabled in production bundle')
process.exit(0)
