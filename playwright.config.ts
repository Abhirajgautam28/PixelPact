// playwright.config.ts - disabled in production cleanup
// Playwright e2e configuration is intentionally removed from production
// distribution to keep the repository minimal. If you need E2E tests,
// restore this file from version control or the development branch.
export default {}
