# Example environment variables for PixelPact
# Server
PORT=3001

# JWT secret used by application auth endpoints (dev default)
JWT_SECRET=dev-secret

# Admin: legacy token (kept for compatibility)
ADMIN_TOKEN=dev-admin-token

# Admin: password to exchange for a JWT via /api/admin/login
ADMIN_PASSWORD=dev-password
ADMIN_JWT_SECRET=dev-jwt-secret

# Optional: MongoDB connection (if you want persistence)
# Example Atlas connection string (replace <db_password> with your DB user password):
# NOTE: URL-encode any special characters in the password (e.g. @ -> %40)
# Example:
# MONGO_URI=mongodb+srv://abhirajgautam28:<db_password>@cluster0.vjodose.mongodb.net/?appName=Cluster0
# Alternatively, some hosting providers (like Vercel) prefer the name MONGODB_URI
# MONGODB_URI=

# When deploying to Vercel, add these environment variables in the Project Settings
# - `MONGODB_URI` or `MONGO_URI` (your Atlas connection string)
# - `JWT_SECRET` (production JWT secret)
# - `FRONTEND_ORIGIN` (optional, e.g. https://your-deployment.vercel.app)
