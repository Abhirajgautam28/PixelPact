# Example environment variables for PixelPact server
# Replace with your MongoDB Atlas connection string when ready
MONGO_URI=mongodb://localhost:27017/
JWT_SECRET=replace-me-with-secure-random
PORT=3001
